# Runtime dependencies for the Streamlit dashboard (what Streamlit Community
# Cloud installs). Kept lean on purpose — the dashboard reads precomputed marts
# and analysis JSON, so it needs neither dbt, Great Expectations, nor the
# modeling libraries. Loose lower bounds so it resolves on any recent Python.
#
# Full local/CI environment (ingestion, dbt, Great Expectations, the analysis
# layer, linting, tests) lives in requirements-dev.txt.
streamlit>=1.38
pandas>=2.0
plotly>=5.23
folium>=0.17
branca>=0.7
google-cloud-bigquery>=3.25
db-dtypes>=1.2
python-dotenv>=1.0
